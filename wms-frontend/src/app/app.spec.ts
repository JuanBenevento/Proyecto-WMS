import { TestBed } from '@angular/core/testing';

describe('App', () => {
  // Placeholder test - main app uses bootstrapApplication
  it('placeholder test', () => {
    expect(true).toBeTrue();
  });
});
